import { FC } from 'react';

const Loading: FC = () => {
	return (
		<div className='flex-1 flex justify-center items-center font-primary bg-white text-primary-dark'>
			<span>Loading..</span>
		</div>
	);
};

export default Loading;
